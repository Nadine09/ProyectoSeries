<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SeriesApp_UI.ViewModels"
             xmlns:en="clr-namespace:SeriesApp_Entities.Classes;assembly=SeriesApp_Entities"
             x:Class="SeriesApp_UI.Views.SeriesDetailsPage"
             Title="SeriesDetailsPage">
    <VerticalStackLayout x:DataType="vm:VM_SeriesDetails">
        <Button Text="Atrás" Command="{Binding GoBackCommand}"/>

        <Label Text="{Binding Series.Id}" />

        <StackLayout>
            <Label Text="{Binding Series.Name}"/>
            <Image Source="{Binding Series.ImageUrl}" MaximumHeightRequest="150"/>
            <Label Text="{Binding Series.Synopsis}"/>
        </StackLayout>
        
        <!--<Button Text="+" Command="{Binding AddSerieCommand}"/>-->
    </VerticalStackLayout>
    
</ContentPage>